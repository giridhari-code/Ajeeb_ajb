function main(): int {
    set out: string = "build/debug_nobool.txt";
    writeFile(out, "");
    set x: int = 5;
    while (x > 0) {
        if (x == 5) {
            writeAppend(out, "FIVE\n");
        }
        else if (x == 3) {
            writeAppend(out, "THREE\n");
        }
        else {
            writeAppend(out, "OTHER\n");
        }
        x = x - 1;
    }
    return 0;
}
