function main(): int {
    set x: int = 0;
    while (x < 5) {
        x = x + 1;
        x = x + 0;
    }
    set out: string = "build/debug_flat.txt";
    writeFile(out, "");
    writeAppend(out, "x=");
    writeAppend(out, itoa(x));
    writeAppend(out, "\n");
    return 0;
}
