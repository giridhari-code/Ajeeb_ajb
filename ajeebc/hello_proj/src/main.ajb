function main(): int {
    println("Hello from Ajeeb via parth!");
    set x: int = 10;
    set y: int = 20;
    println("Sum = " + itoa(x + y));
    set i: int = 0;
    while (i < 5) {
        println("Count: " + itoa(i));
        i = i + 1;
    }
    return 0;
}
