function main(): int {
    set out: string = "build/debug_nestedwhile.txt";
    writeFile(out, "");
    set x: int = 5;
    while (x > 0) {
        if (x == 5) {
            set j: int = 0;
            while (j < 3) { writeAppend(out, "J\n"); j = j + 1; }
        }
        else if (x == 3) {
            writeAppend(out, "THREE\n");
        }
        else {
            writeAppend(out, "OTHER\n");
        }
        x = x - 1;
    }
    return 0;
}
