function inner(): Int {
    set x = 5 / 0;  // division by zero
    return x;
}

function middle(): Int {
    return inner();
}

function outer(): Int {
    return middle();
}

function main(): Int {
    set result = outer();
    println("Result: " + itoa(result));
    return 0;
}
