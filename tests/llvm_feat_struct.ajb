// LLVM Feature Test: Struct (single arg println)
struct Point {
    x: int,
    y: int
}

function main(): int {
    let p = Point { x: 3, y: 4 };
    println(itoa(p.x));
    println(itoa(p.y));
    p.x = 10;
    println(itoa(p.x));
    return 0;
}
