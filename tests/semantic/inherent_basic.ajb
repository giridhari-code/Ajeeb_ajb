struct User {
    name: string,
}

impl User {
    fn greet(self) {
        print("Hello from ");
        println(self.name);
    }
}

function main() {
    set u = User { name: "Ajmal" };
    u.greet();
    println("PASS: inherent_basic");
}
