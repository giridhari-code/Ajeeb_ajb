struct Address {
    city: string
}

struct User {
    name: string,
    address: Address
}

function main(): int {
    set addr = Address { city: "Mumbai" };
    set user = User { name: "Ajeeb", address: addr };
    print(user.name);
    print(user.address.city);
    return 0;
}
