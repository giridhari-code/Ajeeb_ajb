function fib(n: int): int {
    if (n <= 1) { return n; }
    return fib(n - 1) + fib(n - 2);
}
function main(): int {
    let i = 0;
    while (i < 10) {
        println(itoa(fib(i)));
        i = i + 1;
    }
    return 0;
}
