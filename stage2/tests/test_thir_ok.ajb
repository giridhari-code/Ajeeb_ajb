// test_thir_ok.ajb — Valid Ajeeb program (should pass THIR checker)
function add(a: int, b: int): int {
    return a + b;
}
function main(): int {
    set x: int = add(1, 2);
    set msg: string = "Result: ";
    if (x > 0) {
        println(msg + itoa(x) + "\n");
    }
    return 0;
}
